version: 2

models:
  - name: dim_category
    description: Tabela dimensão de categorias.
    columns:
      - name: sk_category
        description: Chave primária da tabela de categorias.
        tests:
          - unique
          - not_null

      - name: category.category_id
        description: Sem crscriçao ainda
        
      - name: category.type
        description: Sem crscriçao ainda
        
      - name: film_category.film_id
        description: Sem crscriçao ainda
        
      - name: film_category.category_id
        description: Sem crscriçao ainda
        